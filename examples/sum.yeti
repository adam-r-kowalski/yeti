fill(ptr: *i32, len: i32) {
  for(0:len) {
    *(ptr + it) = it
  }
}

sum(ptr: *i32, len: i32) {
  total = 0
  for(0:len) {
    total += *(ptr + it)
  }
  total
}

start() {
  ptr = cast(*i32, 0)
  len = 100
  fill(ptr, len)
  sum(ptr, len)
}
