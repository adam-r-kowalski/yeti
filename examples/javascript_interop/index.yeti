draw_square = foreign_import("draw", "square", Fn(length: i64): void)

on_load = fn(): void
  length = 20
  while length < 100 do
    draw_square(length)
    length += 10
  end
end

foreign_export(on_load)
