problem2(upper_bound: i32) {
  previous = 1
  current = 1
  sum = 0
  while(current < upper_bound) {
    sum += (current % 2 == 0) * current
    next = current + previous
    previous = current
    current = next
  }
  sum
}

start() {
  problem2(4000000)
}
