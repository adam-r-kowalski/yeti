min(x: f64, y: f64) {
  if x < y { x } else { y }
}

max(x: f64, y: f64) {
  if x > y { x } else { y }
}

clamp(value: f64, low: f64, high: f64) {
  value.min(high).max(low)
}

start() {
  clamp(3, low=-2, high=2)
}
