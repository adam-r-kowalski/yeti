name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: adamkowalski/zig-docker
    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: zig build test
